---
phase: 1
slug: live-destination-card
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-17
reviewed_at: 2026-06-17
---

# Phase 1 — UI Design Contract: Live Destination Card

> Visual and interaction contract for the Anchor tab card promotion.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> This phase promotes ALREADY-DESIGNED, validated card variants from
> preview/demo-only files into the live AnchorView. The layouts, palette,
> and variant selections are LOCKED from the prior /gsd-sketch workshop.
> Do NOT re-invent the design — implement exactly what is specified below.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (native SwiftUI design tokens) |
| Preset | not applicable |
| Component library | System SwiftUI + iOS 26 Liquid Glass APIs |
| Icon library | SF Symbols (system framework only) |
| Font | System serif (`Font.system(design: .serif)`) for display; System rounded (`Font.system(design: .rounded)`) for body/labels |

**Source:** `Terrarium/DesignSystem/Tokens.swift` and `Terrarium/DesignSystem/GardenTokens.swift` — locked, do not add external dependencies.

---

## Spacing Scale

All values already declared in `Theme.Spacing`. Use only these tokens — no magic numbers.

| Token | Swift Token | Value | Usage in card |
|-------|-------------|-------|---------------|
| xs | `Theme.Spacing.xs` | 4pt | Icon-to-text gaps inside pills |
| sm | `Theme.Spacing.s` | 8pt | Tight insets, pill internal padding |
| md | `Theme.Spacing.m` | 12pt | Card internal element spacing, stub field padding |
| lg | `Theme.Spacing.l` | 16pt | Card outer horizontal padding, header/stub padding |
| xl | `Theme.Spacing.xl` | 24pt | Section separation (card → actions), scrim overlay padding |

All grid-spacing tokens above are multiples of 4. New code must use only these tokens for margins, padding, and gaps.

**Shape geometry constants (NOT spacing tokens).** The following values are
structural dimensions of the custom `TicketShape` / `HTicketShape` (notch radii,
perforation-seam height, stub-section height, art-column width). They are part of
the locked card geometry inherited from `DestinationCardVariants.swift`, not
grid-spacing/padding decisions, and are exempt from the multiple-of-4 spacing rule.
The executor must leave them exactly as-is:

| Constant | Value | Multiple of 4? | Classification |
|----------|-------|----------------|----------------|
| Perforation seam height (ticket) | 22pt | no | shape geometry — frozen |
| Stub section height (ticket) | 66pt | no | shape geometry — frozen |
| Notch radius (ticket shape) | 11pt | no | shape geometry — frozen |
| Notch radius (compact stub) | 9pt | no | shape geometry — frozen |
| Art column width (compact stub) | 112pt | yes | shape geometry — frozen |

**Button vertical padding.** New action-area code uses spacing tokens only.
The primary CTA and secondary buttons use **8pt** (`Theme.Spacing.s`) vertical
padding — NOT the 6pt/4pt raw values seen in earlier workshop code. This is the
one place a non-token value was introduced; it is corrected to the scale here.

**Source:** Extracted from `DestinationCardVariants.swift` and `Tokens.swift`.

---

## Corner Radii

| Token | Swift Token | Value | Usage |
|-------|-------------|-------|-------|
| card | `Theme.Radius.card` | 18pt | Compact stub (`CompactDestinationRow`) outer shape |
| chip | `Theme.Radius.chip` | 12pt | Art band clipping inside cards |
| panel | `Theme.Radius.panel` | 16pt | Supporting panels |
| hero | `Theme.Radius.hero` | 28pt | `TicketDestinationCard` outer shape |
| heroInner | `Theme.Radius.heroInner` | 22pt | Loading skeleton image well |
| glass | `Theme.Radius.glass` | 24pt | Glass-effect containers (action area, state cards) |

---

## Typography

> **Freeze-exemption notice.** This phase promotes an ALREADY-IMPLEMENTED, locked
> design (`TicketDestinationCard` / `CompactDestinationRow` + the live `AnchorView`
> chrome) from preview files into production. It does NOT author a new type scale.
> The contract below is therefore expressed in two parts: (1) the **Core Type Scale**
> — the 4-size / 2-weight design contract the executor builds to, and (2) a
> **Frozen Source-Size Map** documenting how the inherited per-element point sizes
> from the locked components roll into those four tiers. The source sizes are
> frozen from `DestinationCardVariants.swift` / `AnchorView.swift` and are not being
> redesigned in this phase; the executor must NOT introduce sizes outside the four
> core tiers in any NEW code.

Use `Theme.Typography.display()` and `Theme.Typography.body()` factory functions — never raw `.font(.system(...))`.

### Core Type Scale (the contract — 4 sizes, 2 weights)

| Tier | Factory | Size | Weight | Line Height | Usage |
|------|---------|------|--------|-------------|-------|
| Display | `display` | 28pt | `.semibold` | default (~1.2), `minimumScaleFactor` | POI name (primary card) and all heading-class text |
| Body | `body` | 15pt | `.regular` | ~1.5 | Vibe line, descriptions, stub field values, CTA labels |
| Label | `body` | 12pt | `.semibold` | n/a, tracked | Eyebrows, pill text, screen eyebrow |
| Caption | `body` | 9pt | `.semibold` | n/a, tracked | Stub field captions (STATUS / ON FOOT) |

**Two weights only:** `.regular` (base) and `.semibold` (strong). All inherited
`.bold`, `.medium`, and `.heavy` usages map to `.semibold`; everything else maps to
`.regular`. New code must use only these two weights.

### Frozen Source-Size Map (inherited from locked components — do not re-derive)

The locked card/chrome code already ships these exact sizes. They are documented
here for executor fidelity and are exempt from the 4-size rule under the freeze
notice above. The executor leaves these call sites untouched; only NEW typography
must conform to the Core Type Scale.

| Source element | Source size | Source weight | Core tier it represents |
|----------------|-------------|---------------|------------------------|
| Screen heading ("The Hidden Garden") | 30pt | `.bold` | Display (bold one-off override) |
| Card name — ticket | 28pt | `.semibold` | Display |
| Card name — compact stub | 20pt | `.semibold` | Display (compact context) |
| Vibe line / description | 15pt / 14pt / 13pt | `.regular` | Body |
| Stub field value | 15pt | `.semibold` | Body (strong) |
| CTA "Take me there" | 17pt | `.semibold` | Body (large-control override) |
| Re-roll / arrival labels | 15pt | `.medium` → `.semibold` | Body |
| Screen eyebrow / pill text | 13pt | `.semibold` | Label |
| Card eyebrow (ticket / compact) | 11pt / 10pt | `.semibold` | Label |
| Stub field caption | 9pt | `.heavy` → `.semibold` | Caption |

**Source:** `DestinationCardVariants.swift` (lines 68–69, 149–156, 245–268, 349–407), `AnchorView.swift` (lines 119–127, 166).

---

## Color

The approved palette is the **original cream/brown + Garden moss** palette. The Painted Ladies variant (if it existed as a named variant) is CUT and must not appear. The workshop palettes (`coastalFog`, `dusk`, `editorialPop`) are workshop-only — they never touch the live screen.

| Role | Swift Token | Hex | Usage |
|------|-------------|-----|-------|
| Dominant surface (60%) | `Theme.Palette.cardSurface` | `#FFF8EE` | Ticket card fill; compact stub fill |
| Dominant bg top | `Color(hex: "F2E8D2")` | `#F2E8D2` | AnchorView background gradient top stop |
| Dominant bg mid | `Color(hex: "FBF2E0")` | `#FBF2E0` | AnchorView background gradient mid + bottom-fade colour |
| Dominant bg bottom | `Color(hex: "EFF1E2")` | `#EFF1E2` | AnchorView background gradient bottom stop |
| Secondary surface (30%) | `Theme.Palette.chipSurface` | `#FBF2E0` | Loading skeleton fills; pill backgrounds |
| Card border | `Theme.Palette.cardBorder` | `#ECD2AB` | Ticket and compact stub stroke (1pt); dashed perforation rule |
| Title text | `Theme.Palette.title` | `#56392C` | POI name; screen heading; stub field values |
| Secondary text | `Theme.Palette.secondary` | `#917A64` | Vibe/description body text; statusNote text; state card body |
| Label / caption text | `Theme.Palette.label` | `#B08A66` | Eyebrow captions; stub field captions; pill labels |
| Accent (10%) — primary CTA | `Theme.Garden.moss` | `#455528` | "Take me there" button tint; open-status pill tint; header eyebrow color |
| Accent — arrival action | `Theme.Garden.pine` | `#2F5749` | "I'm here" button tint |
| Accent — re-roll action | `Theme.Garden.mossLight` | `#5D6D3E` | "Another" button tint; Discovery eyebrow |
| Accent — walk pill | `Theme.Garden.bloom` | `#FEBB8E` | Walk-time pill tint |
| Postage stamp icon | `Theme.Garden.pine` | `#2F5749` | Category glyph in stamp overlay |

**Accent reserved for:**
- "Take me there" primary CTA button (moss)
- "I'm here" arrival button (pine)
- "Another" re-roll button (mossLight)
- Open-status pill icon + tint (moss)
- Walk-time pill tint (bloom)
- Screen eyebrow text color (mossLight)

**Scrim on ticket art:** `LinearGradient(colors: [.clear, .black.opacity(0.75)], startPoint: .top, endPoint: .bottom)` — applied over `ScenicArtBand` in the ticket header.

**Source:** `Tokens.swift`, `GardenTokens.swift`, `AnchorView.swift` (lines 240–245), `DestinationCardVariants.swift` (lines 336–381).

---

## Card Components: Layout Contract

### Primary — TicketDestinationCard

**Shape:** `TicketShape` — rounded rect (cornerRadius: 28pt) with circular notches punched from each side at the stub seam. Notch radius: 11pt, notch position: 66pt + 11pt from bottom.

**Structure (top to bottom):**

```
┌─────────────────────────────────┐
│  ScenicArtBand (aspectRatio 1.5)│  ← category-driven procedural art
│  [PostageStamp top-trailing]    │  ← 44×52pt, 4pt border-dash, rotated 4°
│  [Scrim overlay bottom-leading] │  ← eyebrow (11pt semibold, tracking 1.4)
│    + POI name (28pt semibold)   │
│    + Vibe line (14pt, optional) │
├ ·  ·  ·  ·  ·  ·  ·  ·  · ────┤  ← DashedRule (1.5pt, dash [5,5]), 22pt zone
│  STUB: STATUS     ON FOOT       │  ← 66pt height, horizontal padding Theme.Spacing.l
│  [openText]       [walkText?]   │  ← 15pt semibold, label 9pt heavy
└─────────────────────────────────┘
```

**Shadow:** `radius: 16, y: 8, opacity: 0.12` (black).
**Stroke:** `Theme.Palette.cardBorder`, 1pt, same `TicketShape`.
**Fill:** `Theme.Palette.cardSurface`.

**Binding fields from AnchorViewModel:**
- `poi.name` → `name`
- `"\(poi.category.label) · \(poi.neighborhood)"` → `eyebrow`
- `viewModel.vibeLine` → `description` (optional; shown in header scrim when non-nil)
- `poi.poiRef` + `poi.category` + `viewModel.context?.weather ?? .clear` → drives `ScenicArtBand`
- `viewModel.pickIsLikelyOpen ? "Open Now" : "Hours vary"` → `openText`
- `viewModel.walkInfo.map { "\($0.walkMinutes) min" }` → `walkText` (optional; stub "ON FOOT" column hidden when nil)

**Accessibility:** `.accessibilityElement(children: .combine)` + `.accessibilityLabel("\(name), \(eyebrow)")`.

---

### Secondary — CompactDestinationRow

**Shape:** `HTicketShape` — rounded rect (cornerRadius: 18pt) with notches punched from top and bottom edges at art column seam. Art column width: 112pt, notch radius: 9pt.

**Structure (leading to trailing):**

```
┌──────────┬────────────────────────────┐
│ ScenicArt│  eyebrow (10pt, tracking   │
│ (112pt)  │  1.2, uppercase)           │
│ [Category│  POI name (20pt semibold,  │
│  Badge   │  lineLimit 1)              │
│  top-    │  Description (13pt,        │
│  leading]│  lineLimit 2, optional)    │
│          │  Pills (open + walk)       │
└──────────┴────────────────────────────┘
```

**Minimum height:** 112pt.
**VDashedRule:** 1.5pt vertical dashed rule at x = 112pt, inset from notch edges by `notchRadius + 5pt`.
**Fill:** `Theme.Palette.cardSurface`.
**Stroke:** `Theme.Palette.cardBorder`, 1pt.
**Shadow:** `radius: 12, y: 6, opacity: 0.10`.

**CategoryBadge (art tab overlay, top-leading):** SF Symbol at 12pt semibold, foreground `Theme.Palette.title`, 7pt padding, `.ultraThinMaterial` circular background, 1pt white 0.5 stroke.

**Usage context in AnchorView:** Not shown in Phase 1 primary flow (no "more nearby" list in the live screen yet). The compact stub component must exist as a production-ready view, bound to real POI data, but it is only wired in the live AnchorView if the screen layout calls for it. Phase 2 determines whether a list is added. For Phase 1 the executor must ensure `CompactDestinationRow` compiles against real data types (not demo data) and renders correctly in a preview.

---

## State Cards: Visual Contract

These cards already exist in `AnchorView.swift` and are NOT being redesigned. They must remain exactly as-is. Listed here for completeness so the executor does not accidentally disturb them.

| State | Component | Trigger |
|-------|-----------|---------|
| Loading | `LoadingHeroCard` | `viewModel.isLoading == true` |
| Pick shown | `TicketDestinationCard` | `viewModel.pick != nil && !isLoading && arrivalResult == nil` |
| Arrived | `ArrivalCard` | `viewModel.arrivalResult != nil` |
| Empty | `EmptyDiscoveryCard` | `viewModel.pick == nil && !isLoading && arrivalResult == nil` |

State card display order in `scrollContent`: loading → arrival card → **ticket card** (new) → empty. The ticket card replaces whatever was previously in the `if let poi = viewModel.pick` branch.

---

## Interaction Contract

### Pinned Action Area

Shown only when `viewModel.pick != nil && !viewModel.isLoading && viewModel.arrivalResult == nil`.

**"Take me there" (primary CTA):**
- Style: `.buttonStyle(.glassProminent)`, tint `Theme.Garden.moss`, `.controlSize(.large)`
- Label: `Label("Take me there", systemImage: "figure.walk")`
- Font: `Theme.Typography.body(17, weight: .semibold)`
- Action: `viewModel.openInMaps()`
- Width: `maxWidth: .infinity`, vertical padding `Theme.Spacing.s` (8pt)

**"Another" (re-roll):**
- Style: `.buttonStyle(.glass)`, tint `Theme.Garden.mossLight`
- Label: `Label("Another", systemImage: "arrow.clockwise")` (visible copy is locked from the workshop decision)
- Accessibility label: `.accessibilityLabel("Another place")` — the visible label is a single word; the icon (`arrow.clockwise`) supplies the verb visually, and this accessibility label supplies the noun for screen readers
- Font: `Theme.Typography.body(15, weight: .semibold)`
- Action: `withAnimation(.smooth) { viewModel.rollAnother() }`
- Width: `maxWidth: .infinity` within HStack, vertical padding `Theme.Spacing.xs` (4pt)

**"I'm here" (arrival):**
- Style: `.buttonStyle(.glass)`, tint `Theme.Garden.pine`
- Label: `Label("I'm here", systemImage: "mappin.and.ellipse")`
- Font: `Theme.Typography.body(15, weight: .semibold)`
- Action: `Task { await viewModel.arrive() }`
- Disabled: `!viewModel.arrivalProximity.allowsCheckIn`
- Width: `maxWidth: .infinity` within HStack, vertical padding `Theme.Spacing.xs` (4pt)

**Status note (above actions):**
- `arrivalBlocked == true` → icon `location.slash.fill` + "Not there yet — get a little closer to check in."
- `arrivalHint != nil` → icon `location.circle` + hint string
- Font: `Theme.Typography.body(13, weight: .semibold)`, color `Theme.Palette.secondary`
- Animation: `.smooth` on `arrivalBlocked` and `arrivalHint` changes

**Action container:** `GlassEffectContainer(spacing: Theme.Spacing.m)` wrapping `VStack(spacing: Theme.Spacing.m)`.

**Bottom fade background:** `LinearGradient` from `Color(hex: "FBF2E0").opacity(0)` → `.opacity(0.92)` → `.opacity(1.0)`, height matched to action area, `allowsHitTesting(false)`.

### Card Transition on Re-Roll

`TicketDestinationCard` must have `.id(viewModel.pick?.poiRef)` so SwiftUI replaces the view identity on re-roll. Transition: `.opacity.combined(with: .scale(scale: 0.97))`. Animation: `.smooth`.

### Reward Overlay

Already wired in `AnchorView`. Not changed by Phase 1. Triggered by `.onChange(of: viewModel.arrivalResult)` when `tiersGained > 0`.

---

## ScenicArtBand Contract

`ScenicArtBand(poiRef:category:weather:)` is the existing procedural art component in `DesignSystem/ScenicArtBand.swift`. It drives the art in both cards. Do not modify it. Both card components already call it correctly.

---

## Copywriting Contract

All copy extracted from existing code — do not change wording.

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA | "Take me there" | `AnchorView.swift:166` |
| Re-roll action | "Another" (visible, locked) · `.accessibilityLabel("Another place")` for screen readers | `AnchorView.swift:181` |
| Arrival action | "I'm here" | `AnchorView.swift:192` |
| Screen eyebrow | "Discovery" | `AnchorView.swift:120` |
| Screen heading | "The Hidden Garden" | `AnchorView.swift:123` |
| Open status (known open) | "Open Now" | `AnchorView.swift:100` |
| Open status (unknown) | "Hours vary" | `AnchorView.swift:100` |
| Stub STATUS caption | "STATUS" | `DestinationCardVariants.swift:388` |
| Stub ON FOOT caption | "ON FOOT" | `DestinationCardVariants.swift:390` |
| Loading state | "Finding your place…" | `AnchorView.swift:357` |
| Empty state heading | "The garden is quiet" | `AnchorView.swift:320` |
| Empty state body | "Nothing's open at this hour. Check back soon, or widen your radius in settings." | `AnchorView.swift:322–324` |
| Empty state CTA | "Try again" | `AnchorView.swift:330` |
| Arrival blocked note | "Not there yet — get a little closer to check in." | `AnchorView.swift:216` |
| Arrival — garden grew heading | "Your garden grew!" | `AnchorView.swift:274` |
| Arrival — points heading | "+{N} points" | `AnchorView.swift:274` |
| Arrival — already visited | "You're here" | `AnchorView.swift:274` |
| Post-arrival CTA | "Find another place" | `AnchorView.swift:297` |

**Destructive actions:** None in Phase 1.

---

## Eyebrow Format

The ticket card eyebrow is formatted by `AnchorView` (not inside the card component itself):

```swift
eyebrow: "\(poi.category.label) · \(poi.neighborhood)"
```

`POICategory.label` values (from `AnchorView.swift`):

| Category | Label |
|----------|-------|
| `.park` | "Park" |
| `.coffee` | "Coffee" |
| `.bookstore` | "Bookstore" |
| `.restaurant` | "Restaurant" |
| `.viewpoint` | "Viewpoint" |
| `.market` | "Market" |
| `.museum` | "Museum" |
| `.bar` | "Bar" |
| `.other` | "Spot" |

---

## SF Symbol → Category Glyph Mapping

Used in `PostageStamp` (ticket card) and `CategoryBadge` (compact stub):

| Category | SF Symbol |
|----------|-----------|
| `.park` | `tree.fill` |
| `.coffee` | `cup.and.saucer.fill` |
| `.bookstore` | `books.vertical.fill` |
| `.restaurant` | `fork.knife` |
| `.viewpoint` | `binoculars.fill` |
| `.market` | `basket.fill` |
| `.museum` | `building.columns.fill` |
| `.bar` | `wineglass.fill` |
| `.other` | `sparkles` |

---

## Data Binding: What Is Real vs. What Must NOT Be Placeholder

Phase 1 success requires ALL of the following fields to come from `AnchorViewModel`, not from demo data:

| Card Field | Source Expression |
|------------|------------------|
| POI name | `poi.name` (from `viewModel.pick`) |
| Eyebrow | `"\(poi.category.label) · \(poi.neighborhood)"` |
| Vibe line | `viewModel.vibeLine` (computed from `pick.vibe` + `context.weather`) |
| Art key | `poi.poiRef`, `poi.category`, `viewModel.context?.weather ?? .clear` |
| Open status | `viewModel.pickIsLikelyOpen ? "Open Now" : "Hours vary"` |
| Walk time | `viewModel.walkInfo.map { "\($0.walkMinutes) min" }` |
| Reward overlay | `viewModel.arrivalResult?.poi.name`, `viewModel.arrivalResult?.poi.category` |

**Blocked:** Any hardcoded string like `"Sutro Baths"`, `"Wave Organ"`, `"poi.sutro-baths.sf"`, or any `DemoSpot` value reaching the live `AnchorView`.

---

## Files In Scope for Phase 1

| File | Change |
|------|--------|
| `AnchorFeature/AnchorView.swift` | Replace old hero card branch with `TicketDestinationCard` bound to real ViewModel state |
| `AnchorFeature/DestinationCardVariants.swift` | Source of truth for both card shapes — no structural edits needed; only confirm it compiles in context |
| `DesignSystem/Tokens.swift` | Read-only — do not modify |
| `DesignSystem/GardenTokens.swift` | Read-only — do not modify |
| `Domain/ExploreModels.swift` | Read-only (frozen contract) — do not modify signatures |

**Files explicitly out of scope for Phase 1:**
- `ExploreProviders.swift` — frozen contract, no changes
- `PaletteWorkshop.swift` — workshop only; DEBUG gating is Phase 2
- `ExperienceFlowDemo.swift` — workshop only; DEBUG gating is Phase 2

---

## Constraints Inherited from CLAUDE.md

- No external dependencies. All components use system SwiftUI + iOS 26 Liquid Glass APIs only.
- `@MainActor` isolation: all ViewModel mutations remain on MainActor.
- Card UI stays in `AnchorFeature/` only.
- Logic stays in `AnchorViewModel` — the card component is purely presentational (no business logic inside `TicketDestinationCard`).
- `ExploreModels.swift` and `ExploreProviders.swift` signatures must not change.
- Minimum deployment: iOS 26.0. `.glassEffect`, `.buttonStyle(.glass)`, `.buttonStyle(.glassProminent)`, `GlassEffectContainer` are all iOS 26 APIs — confirm Xcode 26.0.1 toolchain.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| None | n/a | Not applicable — system frameworks only, no registry |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
