---
phase: quick
plan: 260620-005
type: execute
wave: 1
depends_on: []
files_modified:
  - Terrarium/AnchorFeature/DestinationCard.swift
  - Terrarium/AnchorFeature/DestinationCardVariants.swift
  - Terrarium/AnchorFeature/AnchorView.swift
  - Terrarium/AnchorFeature/AnchorViewModel.swift
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03, CARD-04, WIRE-01]

must_haves:
  truths:
    - "The live Anchor screen renders the TicketDestinationCard as the primary destination card, bound to real AnchorViewModel pick data (D-05, WIRE-01)."
    - "The TicketDestinationCard and CompactDestinationRow live in a production file, not in the workshop variants file (D-01)."
    - "CompactDestinationRow is a production-ready component bound to real POI/AnchorViewModel data with a real-data preview (no hardcoded DemoSpot literals) (D-03, CARD-02)."
    - "The cut variants (Immersive, Postcard) and their demo previews still compile and remain in DestinationCardVariants.swift, untouched, for Phase 2 removal (D-04)."
    - "No Painted Ladies / workshop palette reaches the live screen — the approved cream/brown + Garden-moss palette is used exclusively (CARD-03)."
    - "The app builds against the Xcode 26 toolchain on an iOS 26 simulator and the existing Swift Testing suite passes (verification gate)."
  artifacts:
    - path: "Terrarium/AnchorFeature/DestinationCard.swift"
      provides: "Production home for TicketDestinationCard + CompactDestinationRow + their helper/shape structs + real-data previews"
      contains: "struct TicketDestinationCard"
    - path: "Terrarium/AnchorFeature/AnchorView.swift"
      provides: "Live screen rendering TicketDestinationCard with .id(pick?.poiRef) + re-roll transition"
      contains: "TicketDestinationCard("
    - path: "Terrarium/AnchorFeature/DestinationCardVariants.swift"
      provides: "Residual workshop file: cut variants + demo previews, still compiling"
      contains: "struct ImmersiveDestinationCard"
  key_links:
    - from: "Terrarium/AnchorFeature/AnchorView.swift"
      to: "Terrarium/AnchorFeature/DestinationCard.swift"
      via: "scrollContent pick branch instantiates TicketDestinationCard (now defined in DestinationCard.swift)"
      pattern: "TicketDestinationCard\\("
    - from: "Terrarium/AnchorFeature/AnchorView.swift"
      to: "Terrarium/AnchorFeature/AnchorViewModel.swift"
      via: "card fields bound to viewModel.pick / vibeLine / walkInfo / pickIsLikelyOpen / context.weather"
      pattern: "viewModel\\.(vibeLine|walkInfo|pickIsLikelyOpen)"
---

<objective>
Promote the redesigned **ticket card** (primary) and **compact stub** (secondary) from the preview-only workshop file into a production file, wired to the live `AnchorView` against real `AnchorViewModel` pick data in the approved cream/brown + Garden-moss palette. Faithfully execute locked decisions D-01..D-05 from `01-CONTEXT.md` and the locked binding/visual contract in `01-UI-SPEC.md`.

This is a brownfield, design-locked task. The card layouts, palette, typography, copy, and bindings are FULLY LOCKED — do NOT redesign, re-derive bindings, or expand scope. The only structural change is **relocating** the keeper card structs to a production home and adding `.id(...)` + transition + real-data previews.

Purpose: Phase 2's BUILD-01 wants to gate/remove the workshop files, but `DestinationCardVariants.swift` cannot be gated while the live screen depends on cards defined inside it. Giving the keepers a production home now makes Phase 2 a clean wholesale gate/delete instead of risky surgery.

Output:
- NEW `Terrarium/AnchorFeature/DestinationCard.swift` holding `TicketDestinationCard`, `CompactDestinationRow`, and their helper/shape structs + real-data previews.
- `DestinationCardVariants.swift` reduced to a pure-workshop file (cut variants + demo previews only), still compiling.
- `AnchorView.swift` / `AnchorViewModel.swift` uncommitted wiring reconciled, the re-roll `.id` + transition added, and committed.
</objective>

<execution_context>
@/Users/realtobyfu/.claude/gsd-core/workflows/execute-plan.md
@/Users/realtobyfu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-live-destination-card/01-CONTEXT.md
@.planning/phases/01-live-destination-card/01-UI-SPEC.md
@CLAUDE.md

# Source files in scope (read before editing)
@Terrarium/AnchorFeature/DestinationCardVariants.swift
@Terrarium/AnchorFeature/AnchorView.swift
@Terrarium/AnchorFeature/AnchorViewModel.swift
</context>

<decision_map>
This plan implements the locked decisions. Every task action below cites the decision it satisfies:

- **D-01** — Extract keeper cards + the helper/shape structs they depend on into a NEW production file. Code MOVE only; no edits to card internals/design. (Task 1)
- **D-02** — Deliberate, reconciled deviation: the move extends the UI-SPEC "Files In Scope" table (which said `DestinationCardVariants.swift` needs no structural edits). The SPEC's design intent is preserved — only struct location changes. (Task 1 — note in SUMMARY for any UI-checker re-run)
- **D-03** — `CompactDestinationRow` ships production-ready, bound to real `POI`/`AnchorViewModel` data types, with a real-data `#Preview` (real bundled POI / stub providers — never hardcoded `DemoSpot`/`"Sutro Baths"`). NOT wired onto the live screen in Phase 1. (Task 1)
- **D-04** — Cut variants (`ImmersiveDestinationCard`, `PostcardDestinationCard`) and their demo `#Preview` blocks stay in `DestinationCardVariants.swift`, untouched and compiling. Do NOT delete/gate (that is Phase 2). (Task 1)
- **D-05** — Reconcile/build on the existing uncommitted `AnchorView`/`AnchorViewModel` wiring; confirm bindings match the UI-SPEC data-binding table; add the re-roll `.id` + transition; commit it. (Task 2)
</decision_map>

<file_private_scope_analysis>
**Pre-computed dependency graph (executor: verify, do not re-derive blindly).**

Keeper cards: `TicketDestinationCard` (D), `CompactDestinationRow` (C).

`private` helper/shape struct usage in `DestinationCardVariants.swift`:

| Symbol | Used by keepers? | Used by cut variants / previews? | Disposition |
|--------|------------------|----------------------------------|-------------|
| `TicketShape` | D only | no | MOVE to DestinationCard.swift |
| `HTicketShape` | C only | no | MOVE |
| `CategoryBadge` | C only | no | MOVE |
| `VDashedRule` / `VLine` | C only | no | MOVE |
| `PostageStamp` | D **and** B (Postcard, cut) | yes (B) | **SHARED** — see resolution |
| `DashedRule` / `Line` | D **and** B (Postcard, cut) | yes (B) | **SHARED** — see resolution |
| `glyph(for:)` (free func) | B, C, **D** | yes (B) | **SHARED** — see resolution |

**Why the shared symbols matter:** Swift `private` is file-private scope. If a shared symbol is MOVED out of `DestinationCardVariants.swift`, the cut variants left behind (B `PostcardDestinationCard` and the demo previews) will fail to compile — but D-04 requires them to keep compiling.

**Resolution (locked for the executor — pick approach A):**
- **Keeper-exclusive symbols** (`TicketShape`, `HTicketShape`, `CategoryBadge`, `VDashedRule`, `VLine`) → MOVE into `DestinationCard.swift` as `private`. Delete them from `DestinationCardVariants.swift`.
- **Shared symbols** (`PostageStamp`, `DashedRule`, `Line`, `glyph(for:)`) → **DUPLICATE** as `private` into `DestinationCard.swift` (verbatim copy, zero edits) and **LEAVE the originals in `DestinationCardVariants.swift`** so the cut variants keep compiling. This short-lived duplication is acceptable per the CONTEXT note ("moving shared helpers to the production file and leaving the cut variants referencing them across files is acceptable as long as everything still compiles") and disappears in Phase 2 when the workshop file is removed.
- `OrganicPill` / `SoftPanel` live in `DesignSystem/` (Components.swift / LiquidGlassKit.swift) — NOT in this file. No move, no duplication.
- The `PageBackdrop` preview-backdrop helper and the `StyleComparePreview` are workshop-only (drive the cut-variant previews) → LEAVE in `DestinationCardVariants.swift`. The new file gets its own real-data preview (Task 1).

**Net rendering change: none.** All moved/duplicated structs are byte-for-byte identical; design tokens, shape geometry constants, and call sites are untouched.
</file_private_scope_analysis>

<tasks>

<task type="auto">
  <name>Task 1: Extract keeper cards into production DestinationCard.swift; leave a compiling workshop file</name>
  <files>Terrarium/AnchorFeature/DestinationCard.swift (new), Terrarium/AnchorFeature/DestinationCardVariants.swift</files>
  <action>
Create the production file `Terrarium/AnchorFeature/DestinationCard.swift` (D-01) and relocate the keeper cards into it. This is a code MOVE/COPY only — do NOT edit any card internals, design tokens, shape geometry constants, typography sizes, or copy. Use the pre-computed `file_private_scope_analysis` table above; verify it against the source before moving.

Into the new `DestinationCard.swift` (with `import SwiftUI`), place:
1. `CompactDestinationRow` (currently `DestinationCardVariants.swift` ~L218) — MOVED (cut from the workshop file).
2. `TicketDestinationCard` (~L307) — MOVED.
3. Keeper-exclusive private structs — MOVED (cut from workshop file): `TicketShape`, `HTicketShape`, `CategoryBadge`, `VDashedRule`, `VLine`.
4. Shared private symbols — DUPLICATED (verbatim, leave originals in place): `PostageStamp`, `DashedRule`, `Line`, and the file-private free function `glyph(for:)`. These stay in BOTH files because the cut variant `PostcardDestinationCard` and the demo previews still reference them, and Swift `private` is file-scoped (D-04 requires the cut variants to keep compiling).

In `DestinationCardVariants.swift`:
- DELETE only the two keeper card structs and the five keeper-exclusive private structs listed above.
- LEAVE untouched (D-04): `ImmersiveDestinationCard`, `PostcardDestinationCard`, the shared `PostageStamp`/`DashedRule`/`Line`/`glyph(for:)`, the `PageBackdrop` + `StyleComparePreview` helpers, and ALL existing demo `#Preview` blocks that reference Sutro Baths / Wave Organ. Do NOT delete, DEBUG-gate, or modify the cut variants or their hardcoded-data previews — that is Phase 2 (BUILD-01). Remove the `#Preview("C · Compact list")` and `#Preview("D · Ticket")` blocks that target the moved keeper cards (they move to the new file as real-data previews); keep `#Preview("A · Immersive")`, `#Preview("B · Postcard")`, and `#Preview("Compare · 4 designs")`. Note: `StyleComparePreview` references the moved `CompactDestinationRow` and `TicketDestinationCard` — if leaving it would break compilation across files, trim those two rows out of `StyleComparePreview` (keep A and B) rather than deleting the helper; do this minimally and only if needed to compile.

In `DestinationCard.swift` add real-data `#Preview` blocks for BOTH keeper cards (D-03, CARD-02). The previews MUST be driven by real `POI` data, never hardcoded `DemoSpot`/`"Sutro Baths"`-style literals. Reuse the existing real-data preview pattern from `AnchorView.swift` (the `makePreviewVM` factory wiring `StubPOICatalog` + `StubRecommender` + `InMemoryDiscoveryStore`): build a preview view-model, call `await viewModel.refresh()` in a `.task`, and bind the card fields from `viewModel.pick` exactly as `AnchorView.scrollContent` does — `poi.name`, `"\(poi.category.label) · \(poi.neighborhood)"`, `viewModel.vibeLine`, `viewModel.pickIsLikelyOpen ? "Open Now" : "Hours vary"`, `viewModel.walkInfo.map { "\($0.walkMinutes) min" }`, `viewModel.context?.weather ?? .clear`. Provide a `CompactDestinationRow` real-data preview (required by D-03) and a `TicketDestinationCard` real-data preview (parity, welcome). Use the same `private extension POICategory { var label }` mapping the SPEC §Eyebrow Format requires — if it is file-private to `AnchorView.swift` it is not visible here, so add a file-private copy of just that `label` extension in `DestinationCard.swift` (matches SPEC §Eyebrow Format / §"POICategory.label values" exactly) OR hardcode the eyebrow expression using the category's label via the same switch; do NOT change the live `AnchorView` extension.

Constraints (CLAUDE.md / UI-SPEC): Swift 5, SwiftUI iOS 26, no external deps, `@MainActor`. Card structs stay purely presentational (no business logic moves into them). Do NOT modify frozen contracts `ExploreModels.swift` / `ExploreProviders.swift`. Keep all `Theme.*` tokens and the shape geometry constants (notch radii, 22pt perf seam, 66pt stub height, 112pt art column) exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/realtobyfu/Documents/Terrarium && test -f Terrarium/AnchorFeature/DestinationCard.swift && grep -q 'struct TicketDestinationCard' Terrarium/AnchorFeature/DestinationCard.swift && grep -q 'struct CompactDestinationRow' Terrarium/AnchorFeature/DestinationCard.swift && grep -q 'struct ImmersiveDestinationCard' Terrarium/AnchorFeature/DestinationCardVariants.swift && grep -q 'struct PostcardDestinationCard' Terrarium/AnchorFeature/DestinationCardVariants.swift && ! grep -q 'struct TicketDestinationCard' Terrarium/AnchorFeature/DestinationCardVariants.swift && ! grep -qiE 'DemoSpot|Sutro Baths|Wave Organ' Terrarium/AnchorFeature/DestinationCard.swift && echo PASS</automated>
  </verify>
  <done>
`DestinationCard.swift` exists and contains both keeper cards, their keeper-exclusive shapes/helpers, duplicated shared helpers, and real-data previews (no hardcoded demo literals). `DestinationCardVariants.swift` no longer contains the keeper cards but still contains both cut variants and their helpers/previews. The grep gate above prints PASS. (Final cross-file compilation is proven in Task 2's build gate — the file-private-scope risk is explicitly the build step, not this grep.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile live wiring (add re-roll id + transition), then build + test gate</name>
  <files>Terrarium/AnchorFeature/AnchorView.swift, Terrarium/AnchorFeature/AnchorViewModel.swift</files>
  <action>
Reconcile and build on the existing UNCOMMITTED working-tree wiring (D-05) — do NOT re-derive or revert it. The working tree already replaces the old hero branch in `AnchorView.scrollContent` (~L94-103) with `TicketDestinationCard` and adds the presentation props to `AnchorViewModel` (`vibeLine`, `walkInfo`, `arrivalProximity`, `arrivalHint`, `pickIsLikelyOpen`).

Steps:
1. CONFIRM the existing `scrollContent` `else if let poi = viewModel.pick` branch bindings match the UI-SPEC §"Data Binding" table EXACTLY: `poi.name` → name; `"\(poi.category.label) · \(poi.neighborhood)"` → eyebrow; `viewModel.vibeLine` → description; `poi.poiRef` + `poi.category` + `viewModel.context?.weather ?? .clear` → ScenicArtBand; `viewModel.pickIsLikelyOpen ? "Open Now" : "Hours vary"` → openText; `viewModel.walkInfo.map { "\($0.walkMinutes) min" }` → walkText. They already match — verify, do not rewrite.
2. ADD the locked re-roll identity + transition the working tree is missing (UI-SPEC §"Card Transition on Re-Roll"): attach `.id(viewModel.pick?.poiRef)` and `.transition(.opacity.combined(with: .scale(scale: 0.97)))` to the `TicketDestinationCard` in `scrollContent`. The re-roll already runs inside `withAnimation(.smooth)` (action area "Another" button), so no animation wrapper change is needed.
3. Do NOT touch the state cards (`LoadingHeroCard`, `ArrivalCard`, `EmptyDiscoveryCard`), the pinned action area, the reward overlay, or any `AnchorViewModel` business logic — they are out of scope / Phase 2. The button vertical-padding token reconciliation called out in the SPEC (6pt/4pt → `Theme.Spacing.s`) is NOT in this phase's scope (the action area is a Phase 2 concern); leave existing button padding as-is unless it blocks compilation.
4. Verify cross-file compilation: this is where the Task 1 file-private-scope risk is proven out — both `DestinationCard.swift` and the residual `DestinationCardVariants.swift` must compile together with `AnchorView.swift` now sourcing `TicketDestinationCard` from the new file.

Then run the build + existing-test gate (project: `Terrarium.xcodeproj`, scheme: `Terrarium`, iOS 26 simulator — e.g. `iPhone 17 Pro` on iOS 26.0). Do NOT add new tests for this UI-move task. Fix any compilation error surfaced (most likely a missed shared-symbol duplication from Task 1) before considering the task done.
  </action>
  <verify>
    <automated>cd /Users/realtobyfu/Documents/Terrarium && grep -q '\.id(viewModel\.pick?\.poiRef)' Terrarium/AnchorFeature/AnchorView.swift && grep -q 'scale(scale: 0.97)' Terrarium/AnchorFeature/AnchorView.swift && ! grep -qiE 'DemoSpot|"Sutro Baths"|"Wave Organ"|poi\.sutro-baths\.sf' Terrarium/AnchorFeature/AnchorView.swift && xcodebuild build test -project Terrarium.xcodeproj -scheme Terrarium -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.0' -quiet 2>&1 | tail -25 && echo BUILD_TEST_DONE</automated>
  </verify>
  <done>
The live `AnchorView` renders `TicketDestinationCard` (sourced from the new production file) bound to real ViewModel pick data, with `.id(viewModel.pick?.poiRef)` and the `.opacity.combined(with: .scale(0.97))` re-roll transition present. No hardcoded demo literals reach `AnchorView`. `xcodebuild build test` succeeds on the iOS 26 simulator (app compiles, both card files compile together, the existing Swift Testing suite passes). The grep gate prints `BUILD_TEST_DONE` with a clean `** TEST SUCCEEDED **` (or `** BUILD SUCCEEDED **` + passing tests) in the tail output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational UI relocation + wiring; no new untrusted input crosses any boundary. All data flows from in-process `AnchorViewModel` state already vetted upstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | Card struct relocation (file-private scope) | mitigate | No rendering/logic change: byte-for-byte struct moves/duplication; cross-file compilation proven by the Task 2 `xcodebuild build test` gate. |
| T-quick-02 | Information disclosure | Real-data previews | accept | Previews use stub providers + bundled SF POIs only (no PII, no secrets); previews are DEBUG/preview-only and never ship in release. |
| T-quick-SC | Tampering | npm/pip/cargo installs | accept | No package installs — system frameworks only (CLAUDE.md: no external SPM/CocoaPods deps). No supply-chain surface in this task. |
</threat_model>

<verification>
Phase-level checks:
1. `Terrarium/AnchorFeature/DestinationCard.swift` exists and is the production home of `TicketDestinationCard` + `CompactDestinationRow` (D-01).
2. `DestinationCardVariants.swift` retains both cut variants + their demo previews and still compiles (D-04).
3. `CompactDestinationRow` has a real-data preview with zero hardcoded demo literals (D-03 / CARD-02).
4. Live `AnchorView` binds `TicketDestinationCard` to real VM data with `.id` + re-roll transition (D-05 / WIRE-01 / CARD-01).
5. Approved cream/brown + Garden-moss palette only — no Painted Ladies / workshop palette on the live screen (CARD-03 / CARD-04).
6. `xcodebuild build test -scheme Terrarium` on iOS 26 simulator: app builds and the existing test suite passes.
</verification>

<success_criteria>
- All four files compile together against the Xcode 26 toolchain on an iOS 26 simulator.
- The existing Swift Testing suite passes (no new tests added).
- Keeper cards relocated to production; cut variants left intact and compiling for Phase 2.
- Live Anchor screen renders the ticket card bound to real pick data with the locked re-roll identity/transition.
- D-01..D-05 fully satisfied; the D-02 SPEC "Files In Scope" deviation is noted in the SUMMARY for any UI-checker re-run.
- Working-tree changes committed (atomic per task).
</success_criteria>

<output>
Create `.planning/quick/260620-005-phase-1-promote-ticket-compact-stub-card/260620-005-SUMMARY.md` when done.
</output>
